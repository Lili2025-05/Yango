import 'package:flutter/material.dart';

class YangoPayPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Course')),
      body: Center(child: Text('Bienvenue sur la page Course')),
    );
  }
}
