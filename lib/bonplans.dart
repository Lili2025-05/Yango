import 'package:flutter/material.dart';


class BonPlansPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Bon Plans')),
      body: Center(child: Text('Bienvenue sur la page Bon Plans')),
    );
  }
}
